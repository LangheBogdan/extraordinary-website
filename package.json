{
  "name": "minimalist-portfolio-website",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp -rv css js images *.html dist"
  },
  "devDependencies": {
    "prettier": "3.7.4"
  }
}
